{
  "name": "poker-hand",
  "version": "1.0.0",
  "description": "application that identifies a poker hand",
  "main": "index.js",
  "scripts": {
    "test": "LOG_HIDE_META=true nyc mocha ./test/unit",
    "coverage": "nyc --reporter=text-summary npm run test",
    "style-fix": "standard --fix --env mocha src/**/*.js test/**/*.js",
    "lint": "standard --env mocha src/**/*.js test/**/*.js"
  },
  "keywords": [],
  "author": "Shikhar",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.1.4",
    "nyc": "^14.1.1",
    "standard": "^12.0.1"
  }
}
